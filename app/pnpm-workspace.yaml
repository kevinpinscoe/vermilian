nodeLinker: hoisted
blockExoticSubdeps: false
allowBuilds:
  electron-winstaller: true
  esbuild: true
  # Native deps of @electron-forge/maker-dmg (macOS .dmg). pnpm 11 blocks their
  # build scripts by default, which exits non-zero (ERR_PNPM_IGNORED_BUILDS).
  fs-xattr: true
  macos-alias: true
# Vitest 2.x otherwise pulls a nested Vite 8 (rolldown/oxc) whose SSR transform
# is incompatible with it (ReferenceError: __vite_ssr_exportName__). Pin the
# whole tree to the Vite 5 the app already builds with.
overrides:
  vite: ^5.4.21
  # Security (Dependabot) — pin patched versions of transitive dev-toolchain
  # deps. None of these ship in the packaged app (electron-forge bundles only
  # `dependencies`); they are build/test/CLI tooling only.
  undici: ^7.28.0
  tmp: ^0.2.6
  tar: ^7.5.18
  js-yaml: ^4.2.0
  "@babel/core": ^7.29.6
  postcss: ^8.5.10
